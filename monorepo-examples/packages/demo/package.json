{
  "name": "@asciidoc-prosemirror/demo",
  "version": "0.0.0",
  "private": true,
  "description": "Demo application for AsciiDoc ProseMirror editor",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@asciidoc-prosemirror/vanillajs": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/prosemirror-dev-tools": "^3.0.6",
    "typescript": "^5.8.3",
    "vite": "^7.1.2"
  }
}
